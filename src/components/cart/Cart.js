import '../../styles/app/Cart.css';
import TopBar from "../common/TopBar";

const Cart = () => {
    return (
        <div className="Cart">
            <TopBar />
        </div>
    );
};

export default Cart;
